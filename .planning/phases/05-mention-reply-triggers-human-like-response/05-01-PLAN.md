---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - src/feats/autoChat.ts
autonomous: true
requirements:
  - AUTOCHAT-03
  - AUTOCHAT-04
  - AUTOCHAT-05

must_haves:
  truths:
    - "Bot only responds when @mentioned or replied-to in autoChatChannelID by a sender in autoChatBotIDs"
    - "Bot displays sendTyping + waits 5-15s random delay before replying"
    - "Gemini response includes @mentions of other bots; if no mention, bot replies to triggering message"
    - "Messages from unknown users are completely ignored (zero footprint)"
  artifacts:
    - path: "src/feats/autoChat.ts"
      provides: "Rewritten AutoChatManager with queue, burst typing, mention extraction, and response routing"
      exports: ["AutoChatManager", "default AutoChatManager"]
    - path: "src/typings/typings.ts"
      provides: "Configuration interface with autoChatBotIDs (already exists from Phase 4)"
      contains: "autoChatBotIDs?: string[]"
  key_links:
    - from: "src/feats/autoChat.ts setupMessageListener()"
      to: "autoChatBotIDs Set"
      via: "sender validation before enqueue"
      pattern: "botIDs\\.has\\(message\\.author\\.id\\)"
    - from: "src/feats/autoChat.ts processQueue()"
      to: "geminiService.generateResponseWithInstruction()"
      via: "LLM call with system instruction"
      pattern: "generateResponseWithInstruction"
    - from: "src/feats/autoChat.ts sendResponse()"
      to: "message.reply() / channel.send()"
      via: "mention-count-based routing"
      pattern: "validMentions\\.length === 0"
---

<objective>
Rewrite `AutoChatManager` in `src/feats/autoChat.ts` to implement mention/reply-triggered responses with strict bot-only sender validation, burst typing indicators, randomized 5-15s delay, FIFO queue with deduplication, Gemini mention extraction/validation, and mention-count-based response routing.

Purpose: Replace the disabled `handleMentionOrReply()` stub with a production-ready auto-chat system that only responds to known bots in the shared channel, simulates human-like typing behavior, and lets Gemini decide in-character who to @mention next.
Output: Fully rewritten `src/feats/autoChat.ts` with queue management, burst typing, mention parsing, and response sending.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mention-reply-triggers-human-like-response/05-CONTEXT.md
@.planning/phases/05-mention-reply-triggers-human-like-response/05-RESEARCH.md
@.planning/phases/04-config-schema-personality-injection/04-CONTEXT.md

@src/feats/autoChat.ts
@src/structures/BaseAgent.ts
@src/structures/GeminiService.ts
@src/typings/typings.ts
@src/utils/utils.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/feats/autoChat.ts (current state — TO BE REWRITTEN):
```typescript
export class AutoChatManager {
  private agent: BaseAgent;
  private autoChatChannel?: TextChannel;
  private isProcessingMention: boolean = false;
  // Methods: setupAutoChat(), setupMessageListener(), handleMentionOrReply() [DISABLED],
  //          sendRandomChat() [DISABLED], checkAndSendRandomChat(), setAutoChat(), setAutoChatInterval(), getStats()
}
```

From src/structures/BaseAgent.ts:
```typescript
export class BaseAgent extends Client {
  public config!: Configuration;
  public autoChatManager?: AutoChatManager;
  // config.autoChatBotIDs?: string[] — array of 5 bot user IDs
  // config.autoChatChannelID?: string — shared channel ID
  // config.autoChatCharacter — personality filename
}
```

From src/structures/GeminiService.ts:
```typescript
class GeminiService {
  async generateResponseWithInstruction(prompt: string, systemInstruction: string): Promise<string>;
  // systemInstruction already set by BaseAgent.onReady() with personality + name-to-ID mapping
}
export const geminiService: GeminiService;
```

From src/typings/typings.ts:
```typescript
export interface Configuration {
  autoChatBotIDs?: string[];   // all 5 bot user IDs
  autoChatChannelID?: string;  // shared auto-chat channel
  autoChatCharacter?: string;  // personality filename
  autoChatCharacterName?: string;
  autoChat?: boolean;
  autoChatInterval?: number;
}
```

From src/utils/utils.ts:
```typescript
export const ranInt = (min: number, max: number): number =>
  Math.floor(Math.random() * (max - min + 1)) + min;
```

From discord.js-selfbot-v13 (already installed):
- `message.mentions.users.has(userId: string): boolean` — check if user is mentioned
- `message.reference?.messageId?: string` — referenced message ID for replies
- `channel.sendTyping(): Promise<void>` — typing indicator (expires ~10s server-side)
- `message.reply({ content, allowedMentions }): Promise<Message>` — reply to a message
- `channel.send({ content, allowedMentions }): Promise<Message>` — send standalone message
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AutoChatManager with queue, sender validation, and burst typing</name>
  <files>src/feats/autoChat.ts</files>
  <read_first>
    - src/feats/autoChat.ts (current implementation — entire file to be rewritten)
    - src/structures/BaseAgent.ts (config structure, autoChatBotIDs, autoChatChannelID)
    - src/typings/typings.ts (Configuration interface with autoChat fields)
    - src/utils/utils.ts (ranInt utility)
    - src/structures/GeminiService.ts (geminiService singleton, generateResponseWithInstruction signature)
  </read_first>
  <action>
Rewrite `src/feats/autoChat.ts` AutoChatManager class with the following additions and changes:

**New properties** (per D-12, D-13):
- `private queue: Array<{ message: Message; receivedAt: number }>` — FIFO queue for pending mention triggers
- `private lastProcessed: Map<string, number>` — sender ID → timestamp for 10s dedup window
- `private isProcessing: boolean` — replaces existing `isProcessingMention` flag
- `private readonly MAX_QUEUE = 3` — queue capacity (D-12)
- `private readonly DEDUP_WINDOW_MS = 10_000` — deduplication window (D-13)
- `private abortTyping: (() => void) | null` — optional abort signal for typing loop

**New method: `enqueueTrigger(message: Message): boolean`** (per D-01, D-12, D-13):
- Build `Set<string>` from `this.agent.config.autoChatBotIDs ?? []` for O(1) lookup
- Check sender dedup: if `lastProcessed.get(senderId)` exists and `now - lastTime < 10_000`, find existing queue entry for same sender and replace its message (return `false` — not a new entry)
- If queue length >= 3, `shift()` to drop oldest (D-12)
- Push new trigger, record timestamp in `lastProcessed`, return `true`

**New method: `isTrigger(message: Message): boolean`** (per AUTOCHAT-03, D-04):
- Channel check: `message.channel.id !== this.autoChatChannel?.id` → return false
- Self-message check: `message.author.id === this.agent.user?.id` → return false
- Bot-only validation (D-01): build Set from `autoChatBotIDs`, if `!botIDs.has(message.author.id)` → return false (D-02: silent, zero footprint)
- Mention check: `message.mentions.users.has(this.agent.user.id)` → true
- Reply check: use optional chaining on `message.reference?.messageId` and `this.autoChatChannel.messages.cache.get(refId)?.author.id === this.agent.user?.id` (Pitfall 4 safety)
- Return `isMentioned || isReply`

**Rewrite `setupMessageListener()`** (per D-01, D-04, AUTOCHAT-03):
- Keep existing channel setup logic from `setupAutoChat()`
- In the `messageCreate` handler: call `this.isTrigger(message)` first; if true, call `this.enqueueTrigger(message)` then `void this.processQueue()`
- Remove the old direct `await this.handleMentionOrReply(message)` call

**New method: `simulateBurstTyping(channel: TextChannel, durationMs: number): Promise<void>`** (per D-05, D-07):
- Loop: `while (Date.now() - start < durationMs)`
- Each iteration: `await channel.sendTyping()`, then `await new Promise(r => setTimeout(r, ranInt(1000, 3000)))`
- Support abort: check `this.abortTyping` signal each iteration; if set, break loop and reset signal
- Total duration target: `ranInt(5000, 15000)` (D-07: 5-15s fixed range)

**Rewrite `processQueue()`** (per D-06, D-12):
- Guard: if `isProcessing` is true, return immediately
- Dequeue: `const trigger = this.queue.shift(); if (!trigger) return;`
- Set `isProcessing = true`
- Start Gemini API call immediately: `const geminiPromise = this.agent.geminiService.generateResponseWithInstruction(trigger.message.content, /* system instruction already set in Phase 4 */)`
- Start burst typing in parallel: `const typingPromise = this.simulateBurstTyping(trigger.message.channel as TextChannel, ranInt(5000, 15000))`
- Wait for both: `const [response] = await Promise.all([geminiPromise, typingPromise.catch(() => {})])`
- If another message arrives during delay (D-06): extend by `ranInt(3000, 8000)` — the pending response is NOT cancelled
- After response received: call `this.parseAndValidateMentions(response, botIDs)` (next task) then `this.sendResponse(trigger.message, cleanedText, validMentions)`
- Reset `isProcessing = false`, then call `this.processQueue()` recursively to handle next queued item

**Keep existing methods unchanged**: `setAutoChat()`, `setAutoChatInterval()`, `getStats()`, `checkAndSendRandomChat()` — these are used by the main loop and external config changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/feats/autoChat.ts contains `private queue:` with type `Array<{ message: Message; receivedAt: number }>`
    - src/feats/autoChat.ts contains `private lastProcessed: Map<string, number>`
    - src/feats/autoChat.ts contains `private readonly MAX_QUEUE = 3`
    - src/feats/autoChat.ts contains `private readonly DEDUP_WINDOW_MS = 10_000`
    - src/feats/autoChat.ts contains `enqueueTrigger(` method signature
    - src/feats/autoChat.ts contains `isTrigger(` method signature
    - src/feats/autoChat.ts contains `simulateBurstTyping(` method signature
    - src/feats/autoChat.ts contains `processQueue(` method signature
    - setupMessageListener() calls `this.isTrigger(message)` before any enqueue
    - isTrigger() checks `botIDs.has(message.author.id)` against Set built from `autoChatBotIDs`
    - isTrigger() uses optional chaining on `message.reference?.messageId` and `.cache.get(...)?.author.id`
    - simulateBurstTyping() calls `channel.sendTyping()` inside a while loop with `ranInt(1000, 3000)` pause
    - processQueue() calls `generateResponseWithInstruction` with `trigger.message.content`
    - processQueue() runs typing and Gemini call in parallel via `Promise.all`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>
    AutoChatManager has queue (max 3), dedup (10s per sender), sender validation (Set from autoChatBotIDs), burst typing (5-15s with random 1-3s pauses), and processQueue() that starts Gemini call immediately while typing continues. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement mention extraction, validation, and response routing</name>
  <files>src/feats/autoChat.ts</files>
  <read_first>
    - src/feats/autoChat.ts (after Task 1 changes — current state of the file)
    - src/structures/GeminiService.ts (cleanResponse method, generateResponseWithInstruction output format)
    - src/typings/typings.ts (autoChatBotIDs type)
    - .planning/phases/05-mention-reply-triggers-human-like-response/05-RESEARCH.md (mention extraction regex, response sending patterns)
  </read_first>
  <action>
Add two new methods to `src/feats/autoChat.ts` and wire them into `processQueue()`:

**New method: `parseAndValidateMentions(text: string, knownBotIDs: Set<string>): { cleanedText: string; mentionIDs: string[] }`** (per D-08, D-09, D-10, AUTOCHAT-05):
- Use regex `/<@!?(\d+)>/g` to find all Discord mention patterns in text
- Collect matches via `[...text.matchAll(mentionRegex)]`
- For each match, extract ID (`match[1]`) and check `knownBotIDs.has(id)`
- Build `validMentions: string[]` from IDs that pass validation
- Build `cleanedText` by replacing invalid mention patterns with empty string, then collapsing multiple spaces with `.replace(/\s{2,}/g, ' ')` and `.trim()`
- Return `{ cleanedText, mentionIDs: validMentions }`

**New method: `sendResponse(trigger: Message, text: string, validMentions: string[]): Promise<void>`** (per D-11, AUTOCHAT-05):
- If `validMentions.length === 0`: call `trigger.reply({ content: text, allowedMentions: { repliedUser: false } })` — reply to the triggering message
- If `validMentions.length >= 1`: call `this.autoChatChannel!.send({ content: text, allowedMentions: { users: validMentions } })` — standalone message with mentions
- Handle long responses: if `text.length > 100`, use `geminiService.splitResponse(text)` or similar logic to split into multiple messages (GeminiService already has `splitResponse` as private — alternatively, split by newlines and send each chunk with `ranInt(2000, 5000)` delay between)
- For split messages: first message uses reply/send with mentions as above; subsequent messages use `channel.send()` with same `allowedMentions`

**Wire into `processQueue()`** (from Task 1):
- After receiving Gemini response in processQueue(), call `this.parseAndValidateMentions(response, botIDs)` to get `{ cleanedText, mentionIDs }`
- Then call `this.sendResponse(trigger.message, cleanedText, mentionIDs)`
- Add error handling: if Gemini call fails, log with `logger.error('[AutoChat] Gemini response failed')` and reset `isProcessing` without sending

**Distraction extension (D-06)**:
- In the `messageCreate` handler, after `isTrigger` passes and before `enqueueTrigger`, check if `isProcessing` is true
- If processing, extend the current delay by setting a flag or adding to a running total that `simulateBurstTyping` checks
- The pending response is NOT cancelled — only the visible typing duration extends
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/feats/autoChat.ts contains `parseAndValidateMentions(` method signature
    - parseAndValidateMentions uses regex `/<@!?(\d+)>/g` to extract mention IDs
    - parseAndValidateMentions validates each ID against `knownBotIDs.has(id)`
    - parseAndValidateMentions strips invalid mentions from cleanedText
    - src/feats/autoChat.ts contains `sendResponse(` method signature
    - sendResponse uses `trigger.reply()` when `validMentions.length === 0`
    - sendResponse uses `this.autoChatChannel!.send()` when `validMentions.length >= 1`
    - sendResponse includes `allowedMentions: { repliedUser: false }` for reply case
    - sendResponse includes `allowedMentions: { users: validMentions }` for standalone case
    - processQueue() calls parseAndValidateMentions after Gemini response
    - processQueue() calls sendResponse after mention parsing
    - processQueue() has error handling with `logger.error` for failed Gemini calls
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>
    Gemini response is parsed for `<@ID>` mentions, validated against autoChatBotIDs, invalid mentions stripped. Response sent as reply (0 mentions) or standalone message (1+ mentions) with correct allowedMentions. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration — wire listener, disable legacy random chat, verify build</name>
  <files>src/feats/autoChat.ts</files>
  <read_first>
    - src/feats/autoChat.ts (after Task 1 + Task 2 changes — full current state)
    - src/structures/BaseAgent.ts (main loop calls checkAndSendRandomChat)
    - .planning/phases/05-mention-reply-triggers-human-like-response/05-RESEARCH.md (Pitfall 6: self-message loop, archived library note)
  </read_first>
  <action>
Final integration pass on `src/feats/autoChat.ts`:

**Verify `setupMessageListener()` complete wiring**:
- Confirm the `messageCreate` handler flow: channel check → self-message check → `isTrigger(message)` → `enqueueTrigger(message)` → `void this.processQueue()`
- Ensure no leftover references to the old `handleMentionOrReply` method in the listener
- The old `handleMentionOrReply` method can be removed entirely (was a disabled stub)

**Disable `sendRandomChat()` and `checkAndSendRandomChat()`**:
- `sendRandomChat()` is already a disabled stub returning `Promise.resolve()` — keep as-is
- `checkAndSendRandomChat()` is called by `BaseAgent.main()` loop — it checks `this.agent.config.autoChat` and `this.autoChatChannel` before calling `sendRandomChat()`, so it is harmless. Keep as-is for backward compatibility.

**Verify `getStats()` accuracy**:
- Update `getStats()` to reflect new state: `isProcessingMention` → `isProcessing`, add `queueLength: this.queue.length`
- Remove `botDelayRemaining` and `isBotMessageDelay` fields if no longer relevant (they were for the old delay system)

**Final build verification**:
- Run `npm run build` and confirm zero TypeScript errors
- Verify no unused imports remain (especially any leftover from old implementation)
- Confirm `ranInt` import from `src/utils/utils.ts` is present
- Confirm `TextChannel, Message` imports from `discord.js-selfbot-v13` are present
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - setupMessageListener() does NOT reference `handleMentionOrReply`
    - handleMentionOrReply method is removed from AutoChatManager class
    - getStats() returns `queueLength: this.queue.length`
    - getStats() returns `isProcessing: this.isProcessing` (not `isProcessingMention`)
    - No unused imports in src/feats/autoChat.ts
    - `ranInt` is imported from `../utils/utils.js`
    - `TextChannel, Message` are imported from `discord.js-selfbot-v13`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>
    AutoChatManager is fully wired: message listener uses isTrigger → enqueueTrigger → processQueue flow. Legacy handleMentionOrReply removed. getStats updated with queueLength and isProcessing. TypeScript compiles with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord messageCreate → AutoChatManager | Untrusted message content from Discord; sender ID must be validated against autoChatBotIDs |
| Gemini API response → Discord send | LLM-generated text may contain hallucinated mentions or unsafe content; must be validated and stripped |
| Config JSON → AutoChatManager | autoChatBotIDs and autoChatChannelID from user config; must be validated as non-empty before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | setupMessageListener sender validation | mitigate | Strict Set-based check against autoChatBotIDs (D-01); self-message check runs first (Pitfall 6); non-matching senders silently ignored (D-02) |
| T-05-02 | Tampering | Gemini response → parseAndValidateMentions | mitigate | Regex `/<@!?(\d+)>/g` extraction + validation against autoChatBotIDs Set (D-10); invalid mentions stripped before send |
| T-05-03 | Information Disclosure | logger.error in error paths | mitigate | Error paths log generic failure messages only; no message content or user IDs logged (D-02 zero footprint) |
| T-05-04 | Denial of Service | mention queue overflow | mitigate | Queue capped at MAX_QUEUE=3 (D-12); oldest dropped when full; 10s dedup window per sender prevents same-bot spam (D-13) |
| T-05-05 | Repudiation | message.reference.messageId deleted message | mitigate | Optional chaining on `.cache.get(refId)?.author.id` prevents crash (Pitfall 4); uncached references treated as non-reply |
| T-05-SC | Tampering | npm package installs | mitigate | No new packages — all dependencies already installed and verified (discord.js-selfbot-v13@3.7.1, @google/genai@2.4.0) |
</threat_model>

<verification>
## Phase Verification

### Automated
- `npm run build` — TypeScript compilation passes with zero errors
- No new dependencies added (verify `package.json` unchanged)

### Manual Discord Testing Checklist
1. **Valid mention**: @mention bot from a known bot ID → observe typing indicator (5-15s) → reply received
2. **Valid reply**: Reply to bot's message from a known bot ID → same behavior as mention
3. **Unknown user**: Message from non-bot user → zero response, zero log entry
4. **Non-mention from known bot**: Known bot sends regular message (no @mention, no reply) → no response (D-04)
5. **Queue dedup**: Same bot spams mentions within 10s → only one response generated
6. **Queue cap**: 4+ different bots mention simultaneously → only 3 processed, oldest dropped
7. **Mention extraction**: Gemini includes valid `<@ID>` → sent as standalone message with clickable mentions
8. **Hallucinated mention**: Gemini includes invalid `<@ID>` → stripped from text, sent as reply
9. **Typing persistence**: Typing indicator visible for full 5-15s duration (no 10s flicker)
</verification>

<success_criteria>
1. Bot responds ONLY to @mention or reply from senders in autoChatBotIDs within autoChatChannelID (AUTOCHAT-03)
2. Bot displays sendTyping indicator + waits 5-15s random delay before sending reply (AUTOCHAT-04)
3. Gemini response mentions are extracted via `/<@!?(\d+)>/g`, validated against autoChatBotIDs, and routed correctly: 0 mentions = reply, 1+ mentions = standalone message (AUTOCHAT-05)
4. Messages from unknown users produce zero footprint — no response, no log, no history storage (D-01, D-02)
5. `npm run build` passes with zero TypeScript errors
6. Queue handles max 3 pending mentions with 10s dedup per sender (D-12, D-13)
7. Burst typing refreshes every 1-3s to prevent 10s expiry flicker (D-05)
</success_criteria>

<output>
Create `.planning/phases/05-mention-reply-triggers-human-like-response/05-01-SUMMARY.md` when done
</output>
