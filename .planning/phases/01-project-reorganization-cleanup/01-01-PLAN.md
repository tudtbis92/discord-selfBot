---
phase: 1
plan: 01
title: "Remove Dead Files & Commented-Out Code"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/messageUtils.ts
  - src/typings/quotes.ts
  - src/typings/typings.ts
  - src/feats/autoChat.ts
  - src/feats/command.ts
  - src/structures/BaseAgent.ts
autonomous: true
requirements: [REFACT-03]
---

<objective>
Xoá sạch tất cả dead code: file không được import, type không sử dụng, code block bị comment-out, và method rỗng. Đây là bước đầu tiên vì nó giảm noise cho các bước sau.
</objective>

<must_haves>
truths:
  - "D-02: Xoá sạch vĩnh viễn, không giữ archive (Git lưu lịch sử)"
  - "D-01: Giữ nguyên cấu trúc thư mục kỹ thuật hiện tại"
</must_haves>

<tasks>

<task id="1.1" type="delete">
<title>Xoá file messageUtils.ts không sử dụng</title>
<read_first>
- src/utils/messageUtils.ts
</read_first>
<files>src/utils/messageUtils.ts</files>
<action>
Xoá file `src/utils/messageUtils.ts`. File này không được import bởi bất kỳ file nào trong project (xác nhận qua grep: không có `from ".*messageUtils"` hay `import.*messageUtils` ngoài chính file đó).
</action>
<acceptance_criteria>
- File `src/utils/messageUtils.ts` không tồn tại trên disk
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="1.2" type="delete">
<title>Xoá file quotes.ts không sử dụng (148KB)</title>
<read_first>
- src/typings/quotes.ts
</read_first>
<files>src/typings/quotes.ts</files>
<action>
Xoá file `src/typings/quotes.ts` (1850 dòng, 148KB). File export `quotes` array nhưng không được import bởi bất kỳ file nào khác trong project.
</action>
<acceptance_criteria>
- File `src/typings/quotes.ts` không tồn tại trên disk
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="1.3" type="edit">
<title>Xoá dead types và imports trong typings.ts</title>
<read_first>
- src/typings/typings.ts
</read_first>
<files>src/typings/typings.ts</files>
<action>
Trong `src/typings/typings.ts`:
1. Xoá tất cả import từ `node-notifier` (lines 4-9: `Notification`, `NotificationCenter`, `WindowsToaster`, `WindowsBalloon`, `NotifySend`, `Growl`)
2. Xoá type `popupOptions` (lines 21-26) — không sử dụng ở đâu
3. Xoá type `SendOptions` (lines 15-19) — không sử dụng ở đâu
4. Xoá toàn bộ block comment cũ: lines 69-98 (old `Configuration` interface đã bị comment-out)
</action>
<acceptance_criteria>
- `typings.ts` không còn import từ `node-notifier`
- `typings.ts` không còn export `popupOptions`
- `typings.ts` không còn export `SendOptions`
- `typings.ts` không còn block comment `// export interface Configuration`
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="1.4" type="edit">
<title>Dọn commented-out code trong autoChat.ts</title>
<read_first>
- src/feats/autoChat.ts
</read_first>
<files>src/feats/autoChat.ts</files>
<action>
Trong `src/feats/autoChat.ts`:
1. Method `handleMentionOrReply` (line 60-125): Xoá toàn bộ commented-out code block bên trong. Giữ lại method signature và `return;` statement (method vẫn disabled nhưng clean).
2. Method `sendRandomChat` (line 127-222): Tương tự — xoá toàn bộ commented-out code block. Giữ method signature và `return;`.
3. Xoá `/* eslint-disable no-unreachable */` và `/* eslint-enable no-unreachable */` directives vì không còn unreachable code sau khi dọn comment.
</action>
<acceptance_criteria>
- `autoChat.ts` không còn block comment `//` dài hơn 3 dòng liên tục
- `autoChat.ts` không còn `eslint-disable no-unreachable`
- `handleMentionOrReply` chỉ chứa `return;`
- `sendRandomChat` chỉ chứa `return;`
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="1.5" type="edit">
<title>Dọn commented import trong command.ts</title>
<read_first>
- src/feats/command.ts
</read_first>
<files>src/feats/command.ts</files>
<action>
Xoá dòng comment `// import { logger } from "../utils/logger.js";` (line 6) trong `src/feats/command.ts`.
</action>
<acceptance_criteria>
- `command.ts` không còn import bị comment-out
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0 — project compiles without errors
2. Grep confirm: `grep -r "messageUtils" src/` returns no results
3. Grep confirm: `grep -r "quotes" src/` returns no results  
4. Grep confirm: `grep -r "popupOptions" src/` returns no results
5. Grep confirm: `grep -r "SendOptions" src/` returns no results
</verification>

<success_criteria>
- Tất cả dead files đã bị xoá
- Tất cả dead types đã bị xoá
- Tất cả commented-out code blocks đã được dọn dẹp
- Project vẫn compile thành công
</success_criteria>
