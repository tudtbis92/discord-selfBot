---
phase: 1
plan: 02
title: "Remove Unused Dependencies"
type: execute
wave: 2
depends_on: [01]
files_modified:
  - package.json
autonomous: true
requirements: [REFACT-03]
---

<objective>
Gỡ bỏ tất cả dependencies không còn được sử dụng trong code, bao gồm cả các packages đã bị phát hiện là unused trong quá trình research. Chạy sau Plan 01 vì Plan 01 xoá dead types/imports, khiến thêm dependencies trở nên unused.
</objective>

<must_haves>
truths:
  - "D-03: Quét tự động và gỡ bỏ packages không import/require ở bất kỳ file nào"
  - "D-02: Xoá sạch vĩnh viễn, không giữ lại"
</must_haves>

<tasks>

<task id="2.1" type="execute">
<title>Gỡ bỏ runtime dependencies không sử dụng</title>
<read_first>
- package.json
- src/typings/typings.ts (sau Plan 01 — xác nhận node-notifier imports đã bị xoá)
</read_first>
<files>package.json</files>
<action>
Chạy lệnh uninstall cho các packages đã xác nhận không sử dụng:

```bash
npm uninstall 2captcha axios-cookiejar-support tough-cookie @inquirer/core node-notifier
```

Danh sách packages và lý do:
- `2captcha`: Không import. Chỉ `@2captcha/captcha-solver` được dùng (dynamic import).
- `axios-cookiejar-support`: Zero imports trong `src/`.
- `tough-cookie`: Zero imports trong `src/`.
- `@inquirer/core`: Không import. Chỉ `@inquirer/prompts` được dùng.
- `node-notifier`: Sau Plan 01, import đã bị xoá khỏi typings.ts. Không còn references.
</action>
<acceptance_criteria>
- `package.json` dependencies KHÔNG chứa key `2captcha`
- `package.json` dependencies KHÔNG chứa key `axios-cookiejar-support`
- `package.json` dependencies KHÔNG chứa key `tough-cookie`
- `package.json` dependencies KHÔNG chứa key `@inquirer/core`
- `package.json` dependencies KHÔNG chứa key `node-notifier`
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="2.2" type="execute">
<title>Gỡ bỏ devDependency không sử dụng</title>
<read_first>
- package.json
</read_first>
<files>package.json</files>
<action>
Gỡ `@types/node-notifier` vì `node-notifier` đã bị remove ở task 2.1:

```bash
npm uninstall @types/node-notifier
```
</action>
<acceptance_criteria>
- `package.json` devDependencies KHÔNG chứa key `@types/node-notifier`
- `tsc` compile thành công (exit code 0)
</acceptance_criteria>
</task>

<task id="2.3" type="verify">
<title>Xác nhận toàn bộ remaining deps đều được sử dụng</title>
<read_first>
- package.json
</read_first>
<files>package.json</files>
<action>
Sau khi gỡ dependencies, quét lại toàn bộ remaining dependencies trong `package.json` bằng cách grep từng package name trong `src/` và `index.ts`. Xác nhận mỗi dependency còn lại đều có ít nhất 1 import.

Expected remaining dependencies:
- `@2captcha/captcha-solver` → `src/structures/BaseAgent.ts`
- `@google/genai` → `src/structures/gemini.ts`
- `@inquirer/prompts` → `src/structures/Inquirer.ts`, `src/feats/update.ts`
- `adm-zip` → `src/feats/update.ts`
- `axios` → `src/feats/update.ts`
- `chalk` → `src/utils/logger.ts`
- `commander` → `index.ts`
- `discord.js-selfbot-v13` → multiple files
- `winston` → `src/utils/logger.ts`
</action>
<acceptance_criteria>
- Mỗi dependency còn lại trong `package.json` đều có ít nhất 1 import/require match trong source code
- Không còn orphan dependency nào
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm ls --depth=0` runs successfully without missing peer dependency warnings
3. `node -e "require('./package.json')"` parses without error
</verification>

<success_criteria>
- Đã gỡ sạch 5 runtime deps và 1 devDep không sử dụng
- Tất cả remaining deps đều verified có import match
- Project vẫn compile và npm ls clean
</success_criteria>
