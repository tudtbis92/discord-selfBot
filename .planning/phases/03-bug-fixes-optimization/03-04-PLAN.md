---
phase: 3
plan: 4
title: "Integration Wiring & Final Verification"
type: execute
wave: 3
depends_on: [1, 2, 3]
files_modified:
  - index.ts
  - src/structures/BaseAgent.ts
autonomous: true
requirements:
  - FIX-01
  - FIX-02
---

<objective>
Wire Redis cache and API key rotation into the bot startup flow. Ensure all Phase 3 changes integrate correctly at the application entry point.
</objective>

<tasks>

<task id="03-04-T1" type="execute">
<title>Wire RedisCacheManager and API keys into bot startup</title>
<read_first>
- index.ts
- src/structures/BaseAgent.ts (setConfig method)
- src/structures/RedisCacheManager.ts
- src/structures/GeminiService.ts (setApiKeys method)
- src/typings/typings.ts (Configuration interface)
</read_first>
<action>
In the bot startup flow (BaseAgent.setConfig or index.ts initialization):
1. After config is loaded, if `config.geminiApiKeys` exists and is non-empty, call `geminiService.setApiKeys(config.geminiApiKeys)`.
2. If `config.redisUri` exists, create `new RedisCacheManager(config.redisUri)` and pass it to the ConversationManager via `setRedisCache()`.
3. Add graceful shutdown: on process SIGINT/SIGTERM, call `redisCacheManager.disconnect()`.
</action>
<acceptance_criteria>
- geminiService receives API keys from config on startup
- RedisCacheManager created only when redisUri is configured
- Graceful Redis disconnect on shutdown
- Bot starts normally with no config changes (backward compatible)
- `npx tsc --noEmit` compiles
</acceptance_criteria>
</task>

<task id="03-04-T2" type="execute">
<title>Update handler code to pass history from ConversationManager to GeminiService</title>
<read_first>
- src/handler/ (find files that call chatAsDiscordBot or safeDiscordBotChat)
- src/structures/ConversationManager.ts
</read_first>
<action>
Find all call sites of `chatAsDiscordBot` or `safeDiscordBotChat`. Update them to:
1. Load history from ConversationManager: `conversationManager.getHistory(userId, channelId)`
2. Pass history to `chatAsDiscordBot(userId, message, history)`
3. After response, save to ConversationManager: `conversationManager.addMessage(userId, channelId, 'assistant', response)`
</action>
<acceptance_criteria>
- All chatAsDiscordBot calls pass history from ConversationManager
- No direct access to removed chatHistories Map
- `npx tsc --noEmit` compiles
</acceptance_criteria>
</task>

<task id="03-04-T3" type="execute">
<title>Full build and lint verification</title>
<read_first>
- package.json (build and lint scripts)
</read_first>
<action>
Run `npm run build` to verify full compilation chain (lint + format:check + tsc). Fix any remaining issues.
</action>
<acceptance_criteria>
- `npm run build` exits 0
- No TypeScript compilation errors
- No ESLint errors
- Output in `dest/` directory is up to date
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Bot starts with default config (no Redis, single API key) — no crash
3. All Phase 3 decisions (D-01 through D-08) are implemented
</verification>

<must_haves>
- FIX-01: All minor bugs and dead code resolved
- FIX-02: Optimized flows (key rotation, Redis cache, PM2 logging)
- truths: Backward compatible — bot works with existing config files unchanged
</must_haves>
