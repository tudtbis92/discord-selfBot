---
phase: 3
plan: 3
title: "Redis Conversation Cache with RAM Fallback"
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - src/structures/RedisCacheManager.ts
  - src/structures/ConversationManager.ts
  - src/structures/GeminiService.ts
  - src/typings/typings.ts
  - package.json
autonomous: true
requirements:
  - FIX-02
---

<objective>
Integrate Redis-based conversation caching (D-07) with automatic RAM fallback (D-08). Unify dual history stores into single layered cache.
</objective>

<tasks>

<task id="03-03-T1" type="execute">
<title>Add ioredis dependency and redisUri config</title>
<read_first>
- package.json
- src/typings/typings.ts
</read_first>
<action>
Run `npm install ioredis`. Add `redisUri?: string` to Configuration interface in typings.ts.
</action>
<acceptance_criteria>
- `npm ls ioredis` shows installed
- Configuration has `redisUri?: string`
</acceptance_criteria>
</task>

<task id="03-03-T2" type="execute">
<title>Create RedisCacheManager module</title>
<read_first>
- src/structures/ConversationManager.ts
- 03-RESEARCH.md section 5.2
</read_first>
<action>
Create `src/structures/RedisCacheManager.ts` with: constructor(redisUri?) using ioredis with `enableOfflineQueue: false`, retryStrategy max 10 attempts, event listeners for connect/error/close. Methods: `getHistory(key)`, `setHistory(key, history, ttl=1800)`, `deleteHistory(key)`, `disconnect()`. All try/catch — never throws. Import `Redis from 'ioredis'` and `logger`.
</action>
<acceptance_criteria>
- File exists with RedisCacheManager class
- enableOfflineQueue: false set
- All methods have try/catch, never throw
- Compiles: `npx tsc --noEmit`
</acceptance_criteria>
</task>

<task id="03-03-T3" type="execute">
<title>Integrate Redis into ConversationManager</title>
<read_first>
- src/structures/ConversationManager.ts
- src/structures/RedisCacheManager.ts
</read_first>
<action>
Add `setRedisCache(cache)` method. On `addMessage`: async write to Redis (fire-and-forget). On `getHistory`: try Redis first if local empty. On `endConversation`/`clearHistory`: also clean Redis. All Redis ops are optional — null cache = current RAM-only behavior.
</action>
<acceptance_criteria>
- setRedisCache method exists
- addMessage writes Redis async without blocking
- With null cache, behavior identical to current
- Compiles cleanly
</acceptance_criteria>
</task>

<task id="03-03-T4" type="execute">
<title>Remove duplicate chatHistories from GeminiService</title>
<read_first>
- src/structures/GeminiService.ts
</read_first>
<action>
Remove `chatHistories` Map. Refactor `chatAsDiscordBot` to accept optional history parameter. Remove `clearChatHistory`, `clearAllChatHistories`, `getActiveChatSessions`. Update helpers.
</action>
<acceptance_criteria>
- No `chatHistories` in GeminiService
- chatAsDiscordBot accepts optional history
- Compiles cleanly
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles
2. RedisCacheManager.ts exists
3. No chatHistories in GeminiService.ts
</verification>

<must_haves>
- D-07: Redis caching with TTL
- D-08: Instant RAM fallback via enableOfflineQueue: false
- truths: ConversationManager timeouts/cleanup/cap unchanged, HUONG personality unchanged
</must_haves>
