---
phase: 3
plan: 1
title: "Dead Code Removal, Update Simplification & Logger Optimization"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feats/update.ts
  - src/utils/logger.ts
  - package.json
autonomous: true
requirements:
  - FIX-01
---

<objective>
Remove dead auto-farm dependencies, simplify self-update to Git-only (D-02), and strip file-based logging for PM2-native stdout/stderr (D-06). Also removes captcha-related types (D-01).
</objective>

<tasks>

<task id="03-01-T1" type="execute">
<title>Remove auto-farm and manual-update dead code from update.ts</title>
<read_first>
- src/feats/update.ts
- src/utils/utils.ts (check if copyDirectory is used elsewhere)
</read_first>
<action>
In `src/feats/update.ts`:
1. Remove imports: `AdmZip` (line 8), `copyDirectory` (line 9), `os` (line 3).
2. Remove entire `manualUpdate` method (lines 90-108).
3. Rewrite `performUpdate` to: check `.git` exists → check `git --version` → call `gitUpdate()`. If `.git` missing, log warning via `logger.warn('No .git directory found — skipping auto-update.')` and return. If git not found, log warning and return. No fallback to manual.
4. Keep `checkUpdate()`, `gitUpdate()`, `installDependencies()`, `restart()` intact.
5. Remove the unused `path` import if no longer referenced after changes (it is used in `checkUpdate` for `path.join` so keep it).
6. Remove `fs` import only if not used after removing manualUpdate (it IS used in `performUpdate` for `fs.existsSync('.git')` so keep it).
</action>
<acceptance_criteria>
- `src/feats/update.ts` contains no reference to `AdmZip`, `adm-zip`, `manualUpdate`, `copyDirectory`, or `os.tmpdir`
- `performUpdate` checks for `.git` directory and git binary, logs warning and returns if either missing
- `gitUpdate` method unchanged (stash → pull --force → reset --hard)
- File compiles with `npx tsc --noEmit src/feats/update.ts`
</acceptance_criteria>
</task>

<task id="03-01-T2" type="execute">
<title>Remove dead dependencies from package.json</title>
<read_first>
- package.json
</read_first>
<action>
Remove from `dependencies`:
- `@2captcha/captcha-solver`
- `adm-zip`

Remove from `devDependencies`:
- `@types/adm-zip`

Run `npm install` to update package-lock.json.
</action>
<acceptance_criteria>
- `npm ls @2captcha/captcha-solver` outputs empty (not found)
- `npm ls adm-zip` outputs empty (not found)
- `npm install` completes without errors
- package.json `dependencies` does not contain `@2captcha/captcha-solver` or `adm-zip`
- package.json `devDependencies` does not contain `@types/adm-zip`
</acceptance_criteria>
</task>

<task id="03-01-T3" type="execute">
<title>Remove captcha-related configuration types</title>
<read_first>
- src/typings/typings.ts
</read_first>
<action>
In `src/typings/typings.ts`, remove the captcha configuration fields from the `Configuration` interface:
- Remove line 44 comment `// Captcha Solver Configuration`
- Remove `captchaService?: '2captcha' | 'capmonster' | 'anti-captcha' | 'custom';`
- Remove `captchaKey?: string;`
- Remove `captchaRetry?: number;`

Grep entire `src/` for any references to `captchaService`, `captchaKey`, `captchaRetry` and remove them.
</action>
<acceptance_criteria>
- `src/typings/typings.ts` `Configuration` interface has no captcha-related fields
- `grep -r "captchaService\|captchaKey\|captchaRetry" src/` returns 0 matches
</acceptance_criteria>
</task>

<task id="03-01-T4" type="execute">
<title>Strip file transport from Winston logger</title>
<read_first>
- src/utils/logger.ts
</read_first>
<action>
In `src/utils/logger.ts`:
1. Remove the `fileFormat` definition (lines 36-44 — the `printf` block for file output).
2. Remove the `transports.File({...})` block (lines 63-70).
3. Remove `uncolorize` from the destructured `format` import on line 10 (only used by fileFormat).
4. Add `stderrLevels: ['error', 'alert']` to the Console transport config so PM2 separates error logs.
5. Keep all other code unchanged: custom levels, consoleFormat, chalk colors, singleton pattern.
</action>
<acceptance_criteria>
- `src/utils/logger.ts` has no `transports.File` instance
- `src/utils/logger.ts` has no `fileFormat` variable
- `src/utils/logger.ts` does not import `uncolorize`
- Console transport includes `stderrLevels: ['error', 'alert']`
- File compiles with `npx tsc --noEmit src/utils/logger.ts`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — full project compiles without errors
2. `npm run lint` — no new lint errors introduced
3. `grep -r "adm-zip\|@2captcha\|manualUpdate\|captchaService\|captchaKey" src/` — returns 0 matches
4. `grep -r "transports.File" src/` — returns 0 matches
</verification>

<success_criteria>
- All dead code (captcha solver, adm-zip, manual update) removed
- Self-update uses Git-only with graceful fallback to skip
- Logger outputs to Console only with PM2-compatible stderr routing
- Project compiles and lints cleanly
</success_criteria>

<must_haves>
- D-01: Auto-farm / captcha dependencies and types fully removed
- D-02: update.ts uses Git-only, no ZIP/manual fallback
- D-06: logger.ts has Console transport only, no File transport
- truths:
  - ES Module imports with `.js` extensions preserved
  - Winston custom log levels unchanged
  - gitUpdate() stash/pull/reset sequence unchanged
</must_haves>
