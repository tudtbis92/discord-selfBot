---
phase: 04-config-schema-personality-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/typings/typings.ts
  - src/config/personalities/huong.txt
autonomous: true
requirements:
  - AUTOCHAT-01
user_setup: []

must_haves:
  truths:
    - "Configuration interface accepts autoChatCharacter, autoChatCharacterName, autoChatBotIDs as optional fields"
    - "defaultConfig includes sensible defaults for new autoChat fields"
    - "Personality directory exists with at least one example .txt file"
  artifacts:
    - path: "src/typings/typings.ts"
      provides: "Extended Configuration interface with 3 new optional autoChat fields"
      exports: ["Configuration", "defaultConfig"]
    - path: "src/config/personalities/huong.txt"
      provides: "Example personality text file for Hương character"
      contains: "Vietnamese personality description text"
  key_links:
    - from: "src/typings/typings.ts"
      to: "src/structures/BaseAgent.ts"
      via: "Configuration import"
      pattern: "import.*Configuration.*from.*typings"
    - from: "src/typings/typings.ts"
      to: "src/structures/Inquirer.ts"
      via: "Configuration import"
      pattern: "import.*Configuration.*from.*typings"
---

<objective>
Extend the TypeScript `Configuration` interface with three new optional autoChat fields (`autoChatCharacter`, `autoChatCharacterName`, `autoChatBotIDs`) and create the `src/config/personalities/` directory with an example personality file.

Purpose: Establish the type contract and file storage location that all subsequent autoChat features depend on (per D-01, D-05, AUTOCHAT-01).
Output: Updated typings.ts, new personalities directory with huong.txt
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-schema-personality-injection/04-CONTEXT.md
@.planning/phases/04-config-schema-personality-injection/04-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/typings/typings.ts (CURRENT state — to be EXTENDED):
```typescript
export interface Configuration {
  username: string;
  token: string;
  guildID: string;
  channelID: string[];
  adminID?: string;
  prefix?: string;
  showRPC: boolean;

  // Auto Chat với Gemini AI
  autoChatChannelID?: string;
  autoChat?: boolean;
  autoChatInterval?: number; // phút

  // Avatar Update Channel
  avatarUpdateChannelID?: string;

  // Gemini API Configuration
  geminiApiKey?: string;
  geminiApiKeys?: string[];

  // Redis Cache Configuration
  redisUri?: string;
}

export const defaultConfig: Configuration = {
  username: '',
  token: '',
  guildID: '',
  channelID: [''],
  prefix: '!',
  showRPC: true,
  autoChat: false,
  autoChatInterval: 4,
};
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Configuration interface with autoChat fields</name>
  <files>src/typings/typings.ts</files>
  <read_first>
    - src/typings/typings.ts (current Configuration interface and defaultConfig)
    - .planning/phases/04-config-schema-personality-injection/04-CONTEXT.md (decisions D-01, D-04, D-05, D-07)
  </read_first>
  <action>
    Add THREE new optional properties to the `Configuration` interface in `src/typings/typings.ts`, placed after the existing `autoChatInterval` field:

    1. `autoChatCharacter?: string` — filename in `src/config/personalities/` (e.g., `"huong.txt"`). Per D-01, personalities stored as separate text files.
    2. `autoChatCharacterName?: string` — display name for regex prefix cleaning (e.g., `"Hương Nguyễn"`). Per D-04, used to dynamically construct regex to strip bot name prefixes from Gemini responses.
    3. `autoChatBotIDs?: string[]` — array of all 5 bot user IDs, identical across all configs. Per D-07, the bot filters out its own ID programmatically.

    Update `defaultConfig` to include `autoChatCharacter: undefined`, `autoChatCharacterName: undefined`, and `autoChatBotIDs: undefined` (or omit them since they are optional — TypeScript will accept either approach).

    All three fields MUST be optional (`?`) per D-05. If `autoChat` is false or missing, these fields are ignored.

    Do NOT add `autoChatChannelID` to defaultConfig — it already exists as optional in the interface.

    After editing, run `npm run build` to verify TypeScript compiles cleanly. The project has `noUnusedLocals: true` and `noUnusedParameters: true` — ensure no TS6133 errors.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>
      - src/typings/typings.ts contains `autoChatCharacter?: string`
      - src/typings/typings.ts contains `autoChatCharacterName?: string`
      - src/typings/typings.ts contains `autoChatBotIDs?: string[]`
      - npm run build exits with code 0 (no TypeScript errors)
    </manual>
  </verify>
  <acceptance_criteria>
    - src/typings/typings.ts contains exact property `autoChatCharacter?: string`
    - src/typings/typings.ts contains exact property `autoChatCharacterName?: string`
    - src/typings/typings.ts contains exact property `autoChatBotIDs?: string[]`
    - All three properties are marked optional with `?`
    - `npm run build` exits with code 0
    - No TS6133 (unused variable) errors in build output
  </acceptance_criteria>
  <done>Configuration interface extended with 3 new optional autoChat fields, TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create personalities directory and example file</name>
  <files>src/config/personalities/huong.txt</files>
  <read_first>
    - .planning/phases/04-config-schema-personality-injection/04-CONTEXT.md (D-01: personalities in src/config/personalities/)
    - src/structures/GeminiService.ts (lines 216-254: existing HUONG_PERSONALITY_INSTRUCTION constant — use as reference for personality content)
  </read_first>
  <action>
    Create directory `src/config/personalities/` and file `src/config/personalities/huong.txt`.

    The file MUST be saved as UTF-8 encoding (critical for Vietnamese characters like "Hương", "Nguyễn" — see RESEARCH.md Pitfall 5).

    Content: Move the existing `HUONG_PERSONALITY_INSTRUCTION` constant from `src/structures/GeminiService.ts` (lines 216-254) into this text file. Strip the TypeScript template literal backticks and variable interpolation — save as plain text. The personality text should start with "Bạn sẽ đóng vai một cô gái tên Hương..." and end with "Hãy phản hồi như Hương thực sự đang chat trong server cộng đồng này!".

    Do NOT include `HUONG_INITIAL_GREETING` content — that greeting is superseded by the conversation initiator mechanism (Phase 6, AUTOCHAT-07). Leave it in GeminiService.ts for now.

    This file is the source of truth for Hương's personality. GeminiService will load it at runtime via `fs.readFileSync`.
  </action>
  <verify>
    <automated>Test-Path "E:\Saeth\selftBot-owo\src\config\personalities\huong.txt"</automated>
    <manual>
      - File exists at src/config/personalities/huong.txt
      - File contains Vietnamese text starting with "Bạn sẽ đóng vai"
      - File is valid UTF-8 (no garbled characters)
      - File does NOT contain TypeScript syntax (no backticks, no template literals)
    </manual>
  </verify>
  <acceptance_criteria>
    - Directory src/config/personalities/ exists
    - File src/config/personalities/huong.txt exists and is non-empty
    - File content contains string "Bạn sẽ đóng vai một cô gái tên Hương"
    - File content contains string "DreamScape"
    - File content does NOT contain backtick characters (not a JS template literal)
    - File is readable as UTF-8 with correct Vietnamese characters
  </acceptance_criteria>
  <done>Personalities directory created with huong.txt containing Hương's personality as plain UTF-8 text</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file → filesystem read | `autoChatCharacter` filename used in path resolution — potential path traversal if not validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `autoChatCharacter` filename in config | mitigate | Validate filename contains only `[a-zA-Z0-9._-]+` characters; reject `..`, `/`, `\` path traversal sequences before `fs.readFileSync` call in BaseAgent |
| T-04-02 | Information Disclosure | Personality files in `src/config/personalities/` | accept | Files are developer-controlled, not user-uploaded; low risk if repository access is restricted |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase — all dependencies existing. Slopecheck + blocking human checkpoint not triggered. |
</threat_model>

<verification>
- TypeScript compilation succeeds with no errors
- Configuration interface has all 3 new optional fields
- Personalities directory exists with at least one .txt file
- No hardcoded personality constants remain needed (HUONG_PERSONALITY_INSTRUCTION still exists but will be removed in Plan 02)
</verification>

<success_criteria>
- `npm run build` completes with exit code 0
- `src/typings/typings.ts` exports `Configuration` with `autoChatCharacter?: string`, `autoChatCharacterName?: string`, `autoChatBotIDs?: string[]`
- `src/config/personalities/huong.txt` exists with valid UTF-8 Vietnamese personality text
- All new fields are optional (backward compatible per D-05)
</success_criteria>

<output>
Create `.planning/phases/04-config-schema-personality-injection/04-01-SUMMARY.md` when done
</output>
